Okay, so I have some feedback on our agentic processes. I want to take what we've done so far and really kind of enhance it to show some more detailed flows. So right now we have like an ingest, extract, save, which is, you know, not really enough to showcase a full agentic business workflow with decisions and things like that. So we're going to be going at this from a kind of a dental sales theme for our next examples. and I'm going to describe some here that I think will prove relevant for what we're working on. So the first one is just a friendly sales visit where the sales agent goes out to lunch with the dentist. So in this case, this will be a workflow where we're approving entertainment expenses of sort based on whether they fall below a per diem amount, in which case the reimbursement is automatically approved, or if they go above that, then we require human in the loop approval. The next scenario is a maintenance visit So in this case we will have it will happen based on a scheduled scenario which in this case will be mocked The agent schedules the visit and then a human agent will go out, service the equipment, figure out what visit that this lines up with, and then, I'm sorry, the AI will figure out what visit lines up with, and then it will complete the appropriate steps. So we can talk about that flow as well. Then the last flow that I want to talk about is the recall flow. So in this case, a recall is initiated for a given piece of dental equipment. and in many cases this will require ordering some parts. So the agent will order some parts and then once those parts are ordered, then the flow will move to the next step, which is scheduling a maintenance visit to execute the recall. The human agent will complete the recall and then the, but in order for the maintenance visit to proceed, will have a precondition that the part should have arrived. And again, this is all mock data, so there will be maybe, I don't know, a couple seconds at most between the steps.

I want to store these sample flows under .ai slash samples so we can hash out how to build these kind of flows and make our agent system able to understand and execute them with the Claude Agents SDK.

I want to change our Agent Progress View to have a React flow or something that shows the full flowchart style execution of the Agent, rather than the specific wizard steps or whatever. and then in our storage system, we will save the whole execution path so the user can go between the execution steps, see it happen, observe the actual response, and then as the agent executes, we'll have streaming web sockets that come from our backend that will update the state of the flowchart.

The idea is that we can revisit this as the agent executes.

Then the next thing is that the agent should auto run. You shouldn't have a run agent button. This is stupid. The agent should just auto run on incoming data.

We should have a menu for users to launch certain types of test flows, like things we're going to describe in samples. And then we'll have a number of different agent pipelines that will be initiated or initiatable by these different kind of flows.

We will use an inexpensive AI (Qwen 3.5 27b) of some sort to decide based on what flows should be executed. What we'll have is a little mock email system inside our app that will act like an inbox. So in other words, like a user can receive certain types of emails which might set off the recall flow or something like that.

I want to migrate our UI as well to use ShadCN components and Tailwind so we have a rich design system that really makes sense for what we're doing. Again, we're going to kind of morph this into a real solid dashboard flow.

I also want to have a way where in our agent system, we will have a way to have mock users, which we can switch and then they'll be logged in. So they'll serve as different roles. Like they'll serve as the maintenance guy, the sales agent, the admin, and so on.

The final thing I want to have is a debug window on the page where we watched the agent's execution that shows all the actions the agent is taking in a serial flow in tool calls and things like that so we can inspect that as well.